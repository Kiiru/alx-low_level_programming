#include <stdio.h>
#include "main.h"

/**
 * _abs - procedure to check if a char is an alphabet
 * @n: int input
 * Return: return 1 if c is an alphabet else 0
 */
int _abs(int n)
{
	if (n < 0)
		n *= -1;
	return (n);
}

