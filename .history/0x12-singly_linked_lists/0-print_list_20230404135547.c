#include "lists.h"

/**
 * print_list - Operation to print all the elements of a node
 * Return: number of nodes in the list
*/
size_t print_list(const list_t *h)
{
	int i = 0;

	while (h != NULL)
	{
		if ()
	}
}