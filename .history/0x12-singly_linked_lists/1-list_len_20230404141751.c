#include "lists.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * list_len - Operation to print all the nodes of a list
 * @h: list of nodes list_t
 * Return: number of nodes in the list
*/
size_t list_len(const list_t *h)
{
	size_t i = 0
}