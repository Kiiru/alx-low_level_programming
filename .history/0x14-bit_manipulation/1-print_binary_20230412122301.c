#include "main.h"

/**
 * print_binary - Operation to print binary value of int
 * @n: Int input value
 * Return: always void
*/
void print_binary(unsigned long int n)
{
	int i, flag;

	if (n == 0)
		_
}