#include <stdio.h>
#include <string.h>

/**
 * leet - operation to encode string
 * @s: pointer to string array
 * Return: always capitalized String
 *
*/
char *leet(char *s)
{
	int i;

	for (i = 0)
}