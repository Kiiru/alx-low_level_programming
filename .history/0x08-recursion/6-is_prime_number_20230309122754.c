#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * is_prime_number - recursively tries to find out if n is 
 * @start: int value. start of search
 * @end: int value. end of search
 * @m: the square we are searching the root for
 * Return: mid
*/
int is_prime_number(int n)