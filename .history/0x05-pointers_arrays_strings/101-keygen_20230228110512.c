#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - operation body. gen random passwords
 *
 * Return: Always 0.
 */
int main(void)
{
	int _rand, i, t;

	srand ((unsigned) time(NULL));
	for (i = 0, total = 2772; total > 122; count++)
}