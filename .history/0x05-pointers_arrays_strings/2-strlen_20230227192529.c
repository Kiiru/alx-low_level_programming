#include "main.h"
#include <string.h>

/**
 * _strlen - reset value to 98
 * @s: pointer to char array variable s
 * Return: Always returns void.
 */
int _strlen(char *s)
{
	int len;

	len = strlen
}
